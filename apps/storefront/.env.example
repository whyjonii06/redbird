# URL of your Redbird API (leave empty to use relative /trpc — works when served behind the same nginx)
# Set this when the storefront is deployed separately from the API
VITE_API_URL=https://api.yourstore.com

# Stripe publishable key (used for Stripe Elements in checkout)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
